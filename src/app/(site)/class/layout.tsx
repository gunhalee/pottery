import type { ReactNode } from "react";
import "../spec.css";
import "../feedback.css";
import "./class.css";

export default function ClassLayout({ children }: { children: ReactNode }) {
  return children;
}
